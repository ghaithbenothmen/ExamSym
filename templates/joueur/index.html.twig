<!DOCTYPE html>
<html>
	<head>
		<title>Liste
		</title>
		<!-- Ajout des liens vers les fichiers CSS de Bootstrap -->

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
	</head>
	<style>
		.container {
			padding: 2rem 0;
		}

		h1,
		h2,
		h3,
		h4 {
			margin: 2rem 0 1rem;
		}

		.table-image {
			td,
			th {
				vertical-align: middle;
			}
		}
		.btn {
			margin: 5px;

		}
	</style>
	<body>
		<h1>Liste des joueurs</h1>

		<div class="row">
			<div class="col-12">
				<table class="table table-image">
					<thead>
						<tr>
							<th scope="col">
								Nom</th>
							<th scope="col">Equipe</th>
							<th scope="col">Action</th>
						</tr>
					</thead>
					<tbody>
						{# {% if js is empty %}
							<tr>
								<td colspan="6">Le tableau des auteurs est vide</td>
							</tr>
						{% endif %} #}
						{#    {% set totalj = 0 %} #}
						{% for j in joueur %}
							{# {% if j.published %} #}


								<tr>
									
									
									<td>{{ j.nom |upper}}</td>
									<td>{{ j.equipe }}</td>
									
									<td>
										<a href="{{ path('app_joueur_details', {'id': j.id}) }}" class="btn btn-info btn-sm" role="button">Show</a>
{# 										<a href="{{ path('app_joueur_details', {'id': j.id}) }}" class="btn btn-warning btn-sm" role="button">Edit</a> #}
										<a href="{{ path('app_joueur_vote', {'id': j.id}) }}" class="btn btn-danger btn-sm" role="button">Voter</a>
									</td>

									{# <td><a class="btn btn-primary" href="{{ path('jDetails', {'id': j.ref}) }}">Détails</a></td> #}


								</tr>
							{# {% endif %} #}
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>

       
		{# <div class="text-center">
			<a href="{{ path('app_j_add') }}" type="button" class="btn btn-primary">Ajouter equipe</a>
		</div> #}
{# 
		<h2 class="text-primary">Le nombre des livres public est :
			{{ totaljsPub }}</h2>
		<h2>Lenombre des livres non public :
			{{totaljsUnpub}}
		</h2> #}

		<!-- Ajout des liens vers les fichiers JavaScript de Bootstrap (facultatif) -->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/js/bootstrap.min.js"></script>
	</body>
</html>
